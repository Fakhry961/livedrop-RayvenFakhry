### Storefront example env (Vite). Copy to .env.local for local dev or set in Vercel as VITE_API_URL
# Example: VITE_API_URL=https://livedrop-api.onrender.com
VITE_API_URL=https://livedriop-rayvenfakhry.onrender.com

# Notes:
# - VITE_ prefixed variables are embedded at build time by Vite.
# - In Vercel set this variable under Project Settings → Environment Variables (Preview/Production as needed).
# - For local development, create a file `apps/storefront/.env.local` (gitignored) and set VITE_API_URL=http://localhost:3001
