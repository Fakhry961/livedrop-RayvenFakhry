{
  "version": 2,
  "builds": [
    {
      "src": "apps/storefront/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/apps/storefront/$1" }
  ]
}
